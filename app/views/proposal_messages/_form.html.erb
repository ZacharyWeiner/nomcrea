<%= form_for [@proposal, @proposal_message] do |form| %>
  <% if @proposal_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@proposal_message.errors.count, "error") %> prohibited this @proposal_message from being saved:</h2>

      <ul>
      <% @proposal_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="contact-form">
    <%= form.label :message %>
    <%= form.text_area :message, id: :proposal_message_message, rows:4 %>
  </div>

    <%= form.hidden_field :user_id, :value => current_user.id%>
    <%= form.hidden_field :proposal_id, :value =>  @proposal.id  %>

  <div class="actions">
    <%= form.submit "Send", class: 'push-1' %>
  </div>
<% end %>
