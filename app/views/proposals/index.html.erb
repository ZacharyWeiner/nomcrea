<p id="notice"><%= notice %></p>

<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">Available Proposals</h1>
    <p class="lead text-muted">Go Find You Some Work!</p>
    <p>
      <a href="#" class="btn btn-primary">My Work</a>
      <a href="#" class="btn btn-secondary">My Queue</a>
    </p>
  </div>
</section>
<% if current_user && current_user.has_roles?(:company) %>
<div class='container'>
<%= link_to 'New Proposal', proposal_selection_path, class: 'btn btn-primary right' %>
</div>
<br>
<% end %>
<div class="album text-muted">
  <div class="container">
    <div class="row">
      <%= render @proposals%>
    </div>
  </div>
</div>


